- lvm sizes/volumes depend on machine, so should not be in all_server, but in host_vars, per machine
=> splitted vars, but need to update them according to the production machines

- docker nginx on internal machine with internal certificate
- docker nginx on external machine with LetsEncrypt certifcate
- docker wordpress install

  
