- docker postgresql is installed, but security needs attention, with pg_hba module, ssl with internal certificate?
- force ssl to db
- add postgres user to ldap

- docker nginx on internal machine with internal certificate
- docker nginx on external machine with LetsEncrypt certifcate
- docker wordpress install

  
